extends layouts/default

block content
  .ui.cards
    include partials/post-list

    .ui.fluid.card
      .content#submit(data-intent="comment")
        if (user)
          h3 New Post
          include forms/new-post
        else
          include partials/authenticate
