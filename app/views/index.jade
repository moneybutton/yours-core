extends layouts/default

block content

  h2 Forums
  for forum in forums
    include partials/forum-row

  if (user)
    h4 Create Forum
    form(action="/forums", method="POST")
      input(type="text", name="name")
      input.btn(type="submit")
