/**
 * The default export is DattCore, the logic of the datt p2p protocol. DattCore
 * is what you want to use to run a datt node, either in node.js or a web
 * browser. It does not include the UI.
 */
module.exports = require('./core/index')
